# WeatherGPT Frontend Environment Variables
# Configure FastAPI backend gateway URL for production deployment

VITE_API_BASE_URL=http://localhost:8000/api

# Note: Sensitive secrets (IMD API keys, LLM credentials, SMS gateway credentials)
# must NEVER be stored in frontend environment variables. All authorized access is handled by FastAPI.
