[project]
name = "weathergpt-mvp"
version = "0.3.0"
description = "Grounded conversational weather intelligence MVP"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.111",
  "uvicorn[standard]>=0.30",
  "httpx>=0.27"
]

[tool.vercel]
entrypoint = "backend.main:app"
